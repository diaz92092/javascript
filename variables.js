let petName = "Hal"
// let petName = 'Spot" (this is incorrect if renaming(reassigning) don't use let)
petName = "Spot"

console.log(petName)

// 1. You can't define a variable more than once
// 2. There are rules related to variable names
// (number can be first number of character)
// character can start with a-z _ or $
// 3. Variable names cannot be reserved keywords